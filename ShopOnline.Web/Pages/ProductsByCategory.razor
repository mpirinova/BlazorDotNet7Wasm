@page "/productsByCategory/{CategoryId:int}"

@if(Products is null && ErrorMessage is null)
{
    <DisplaySpinner/>
}
else if(ErrorMessage is not null)
{
    <DisplayError ErrorMessage = "@ErrorMessage" />
}
else
{
    <MudText Class="m-4" Typo="Typo.h3">Products (@CategoryName)</MudText>
    @if(Products.Count() > 0)
    {
        <MudContainer class = "row mt-3">
          <DisplayProduct Products = "@Products"></DisplayProduct>
        </MudContainer>
    }
}
